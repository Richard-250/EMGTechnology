import {DashboardFormComponent} from '@vendure/dashboard';

/** Hides native custom-field inputs that are managed by ProductDiscountPanel instead. */
export const HiddenCustomField: DashboardFormComponent = () => null;
